---
id: D-0017
status: active
validated: 2026-09-22
provenance: topology.yaml special_environments.dha; archived steering (DHA topology, commit b611237); corrected 2026-09-18 by Adam's by-hand console access, scope set 2026-09-22; rewritten 2026-09-22 (user: "DHA unreachable for the agent only; and we will work with the assumption that the only DHA account ending in 775 is that")
paths: none
replaces: none
---
**Decided:** The agent has no credentials for DHA and cannot reach it from any profile or CLI. Do not hunt for a DHA profile, do not configure one, and do not assume one will appear. This is a limit on the agent, not a claim that no such account exists: Adam can reach a single DHA GovCloud account by hand, account id ending 775, us-gov-west-1, through AVD plus an Alt token, AWS console only, no CLI. The us-gov-east-1 region is empty. Anything needing live DHA data must be run by Adam by hand and pasted back. Work destined for DHA that can be mirrored is done in titan-sandbox (profile titan-sandbox, us-west-2, tsnbx4 / tsnbx7), and the Leidos/DHA side pulls code and tfvars.

**Because:** Past sessions burned turns looking for a DHA account, so the "stop looking" half stands. But the original wording said no reachable account exists at all, which is false and caused the DHA leg of DVPS-6795 to be written up as impossible before Adam ran it by hand on 2026-09-18. Under the working assumption set on 2026-09-22, that same account is also the single Titan prod account, so calling it unreachable would wrongly mark the prod validation Richard asked for as outstanding.

**Applies when:** Any ticket, repo (azure_titan, cvg-titan-input), or prompt that mentions DHA, titan, or the government environment; and whenever judging whether a live check in prod or DHA is possible.

**Does not apply when:** Azure-side titan work (AVD, VPN gateways), which uses Azure, not the AWS sandbox.

**Replaces:** none
